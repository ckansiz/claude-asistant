node_modules/
.env
.env.*
!.env.example
.DS_Store
*.log
.claude/settings.local.json
tools/image-generator/outputs/
__pycache__/
*.pyc
tools/image-generator/sample

# Project-local work area — used by downstream projects, not the template itself
projects/

# Active sprint folders are project-local; only the templates are shared
docs/sprints/[0-9]*

# Sync lock file (stores upstream SHA of the template this project pulled from)
.eng-team.lock
