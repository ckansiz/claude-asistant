# /frontend — Frontend Mode

First, determine the stack from the project's `package.json` or `CLAUDE.md`:
- **Astro project** → read `.claude/context/astro.md`
- **Next.js project** → read `.claude/context/nextjs.md`

Also read:
- `.claude/context/typescript.md` — TypeScript standards (applies to both)
- `.claude/context/commits.md` — commit conventions

If the project uses a .NET backend, also read `.claude/context/api-contract.md` for type sync workflow.

Delegate implementation to **@builder**.

$ARGUMENTS
