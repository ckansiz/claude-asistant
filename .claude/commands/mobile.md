# /mobile — Mobile Development Mode

Stack decision: **React Native / Expo** (default). Flutter only if the project already uses it.

Activate **@builder**. Skills auto-load — `react-native` (or `flutter`), `typescript`, `commits`, `api-contract` (when applicable) will trigger as needed.

$ARGUMENTS
