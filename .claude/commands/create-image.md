# /create-image — Image Generation Mode

Activate **@image-gen**. The `image-generation` skill auto-loads and drives the Wesoco client special-day visual workflow (always asks client + occasion + emotion + format + style before generating).

$ARGUMENTS
