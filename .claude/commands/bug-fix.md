# /bug-fix — Bug Fix Workflow

Runs the bug orchestration loop: reproduce → root-cause → plan → fix → review → regression test → PR. The `bug-fix`, `orchestration`, `plan-mode` skills auto-load.

**Bug:** $ARGUMENTS
