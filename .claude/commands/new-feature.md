# /new-feature — New Feature Workflow

Runs the end-to-end orchestration loop: analyze → plan → @builder → review → test → PR. The `new-feature`, `orchestration`, `plan-mode` skills auto-load.

**Request:** $ARGUMENTS
