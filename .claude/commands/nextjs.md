# /nextjs — Next.js Mode

Activate **@builder**. Skills auto-load by description match — `nextjs`, `typescript`, `database`, `security`, `testing`, `commits` will trigger as needed. If the project also has a .NET backend, `api-contract` triggers for type sync.

$ARGUMENTS
