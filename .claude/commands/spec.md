# /spec — Specification Mode

Activate **@architect**. The `spec-writing` skill auto-loads — clarifying questions → `docs/requirements.md` → `docs/tech-spec.md`. Implementation only starts after the user approves the spec.

$ARGUMENTS
